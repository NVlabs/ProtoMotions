# MuJoCo CPU-only simulator requirements
#
# Setup instructions:
#   1. Create conda environment:
#      conda create -n protomotions_mujoco python=3.10
#      conda activate protomotions_mujoco
#
#   2. Install PyTorch CPU version (lighter, no CUDA needed):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
#   3. Install ProtoMotions:
#      pip install -e .
#
#   4. Install MuJoCo requirements:
#      pip install -r requirements_mujoco.txt
#
#   5. Run inference (num_envs must be 1):
#      python protomotions/inference_agent.py \
#        --checkpoint results/experiment/last.ckpt \
#        --simulator mujoco \
#        --num-envs 1
#
# Note: MuJoCo backend is CPU-only and single-environment only.
#       Useful for quick testing without GPU or for debugging.
#
mujoco>=3.0.0
dm_control>=1.0  # Required by pose_lib for MJCF parsing
wheel
tensordict==0.9.0
lightning
numpy>=1.21.1
termcolor>=1.1.0
scikit-image
rtree==1.2.0
trimesh
setuptools==69.5.1
rich
typer>=0.6.1
transformers>=4.20.1
matplotlib
easydict
wandb==0.23.0
sentry-sdk==2.0.0
hydra-core==1.3.2
moviepy
pandas
openmesh==1.2.1
tensorboardX
